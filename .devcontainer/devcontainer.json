{
  "name": "Memoize class-decorator",
  "dockerComposeFile": "../docker-compose.yml",
  "service": "lib",
  "workspaceFolder": "/workspace",
  "extensions": ["esbenp.prettier-vscode", "arcanis.vscode-zipfs"],
  "postStartCommand": "yarn install && yarn rebuild && yarn sdks vscode"
}
